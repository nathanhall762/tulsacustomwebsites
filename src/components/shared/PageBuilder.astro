---
import type { Item } from '../../layouts/Layout.astro';
import AboutCard from './sections/AboutCard';
import Accordion from './sections/Accordion';
import BulletStatsCard from './sections/BulletStatsCard';
import Hero from './sections/Hero';
import InfoCardList from './sections/InfoCardList';
import Sidekick from './sections/Sidekick';
import TabListCards from './sections/TabListCards';
import TextImagecard from './sections/TextImageCard';
import Cta from './sections/Cta';
import Testimonial from './sections/Testimonial';
import EuroProjectGallery from '../EuroProjectGallery';
const { pageData } = Astro.props;
---

<>
  {
    pageData.content.map((item: Item) => {
      switch (item.type) {
        case 'hero_section':
          return <Hero {...item.value} client:idle />;
        case 'sidekick':
          return <Sidekick {...item.value} />;
        case 'welcome_message_section':
          return <AboutCard {...item.value} />;
        case 'info_card_list':
          return <InfoCardList {...item.value} />;
        case 'bullet_stats_card':
          return <BulletStatsCard {...item.value} />;
        case 'tab_list_cards':
          return <EuroProjectGallery {...item.value} client:visible />;
        case 'text_image_card':
          return <TextImagecard {...item.value} />;
        case 'accordion':
          return <Accordion {...item.value} client:visible />;
        case 'cta_section':
          return <Cta {...item.value} />;
        case 'testimonial':
          return <Testimonial {...item.value} />;
        default:
          return null;
      }
    })
  }
</>
